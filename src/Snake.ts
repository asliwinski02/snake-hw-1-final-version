import display from "./display";

// place your code on

public class Snake {}
private String currentPosition: position;
private String currentDirection: direction;

move() {
if Snake= "Facing Foward"
 currentPosition="Increase its position by the given number"
 else "Decrease its position by the given number"


}
turn () {
  if Snake="Facing foward"
  currentDirection="Turn it backward"
  else"Turn it foward
}
public get position() {
  return currentPosition
}


export default Snake;
